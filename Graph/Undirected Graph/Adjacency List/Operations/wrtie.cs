using System;

namespace MatrixGraph
{
    public static class Program
    {
        public static void Main()
        {
            var vertexCount = GetPositiveInteger("Enter number of vertices");
            var graph = new Graph(vertexCount);

            while (true)
            {
                Console.WriteLine("\nMain Menu:");

                var choice = 

                switch (choice)
                {
                    case 1: break;
                    default:
                }
            }
        }
    }
}